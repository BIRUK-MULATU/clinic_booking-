# Clinic Booking — frontend

A decorated React (Vite) UI for the clinic booking app, talking to the
Spring Boot backend in `../backend` over `/api/*`. See the repository-root
`README.md` for how to run this alongside the backend, and for what is and
isn't part of the graded test suite.

```bash
npm install
npm run dev
```
